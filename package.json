{
  "name": "portfolio-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Portfolio && npm ci && npm run build",
    "start": "npx serve Portfolio/dist -l ${PORT:-4173}"
  }
}
